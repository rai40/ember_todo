---
applications:
  .:
    name: ember-todo
    framework:
      name: rails3
      info:
        mem: 256M
        description: Rails Application
        exec: 
    infra: eu-aws
    url: ${name}.${target-base}
    mem: '128'
    instances: 1
    services:
      postgresql-ember-todo:
        type: postgresql
